# Implementação da camada de comunicação

def send_message(message, recipient):
    # Enviar mensagem para o destinatário
    # Usar biblioteca de comunicação
    return

def receive_message(sender):
    # Receber mensagem do remetente
    # Usar biblioteca de comunicação
    return